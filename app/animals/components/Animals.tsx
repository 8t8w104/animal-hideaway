'use client';
import React from 'react'

import { Animal } from '@prisma/client';


export const Animals = ({ animals }: { animals: Animal[] }) => {
  return (
    <>
      一覧ページ
    </>
  );
}
